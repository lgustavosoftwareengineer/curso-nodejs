<h2>Lista de categorias</h2>
<hr>
<a href="/admin/categorias/add" class="btn btn-outline-success">Nova categoria</a>
{{#each categorias}}

    <div class="card">
        <div class="card-body">
            <h4>{{nome}}</h4>
            <small>{{slug}}</small><br>
            <small>{{date}}</small>
        </div>
    </div>
    
{{/each}}